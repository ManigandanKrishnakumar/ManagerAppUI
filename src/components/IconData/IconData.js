import React, {useEffect, lazy} from 'react';
import {View, Text} from 'react-native';
import styles from './IconDataStyles';
export default ({iconName, iconFamily, color, size, style, data}) => {
  return (
    <View>
      <Text>Icon Data</Text>
    </View>
  );
};
